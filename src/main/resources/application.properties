spring.application.name=selfpotify

# Carga variables por instalación desde .env en la raíz del proyecto (opcional).
# Las variables de entorno reales del SO tienen prioridad sobre las de .env.
spring.config.import=optional:file:./.env[.properties]

# Puerto del servidor
server.port=${SERVER_PORT:8080}

# Configuración de la Fuente de Datos
spring.datasource.url=${DB_URL:jdbc:h2:file:./data/selfpotify;AUTO_SERVER=TRUE}
spring.datasource.driverClassName=${DB_DRIVER:org.h2.Driver}
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:}

# Consola H2
spring.h2.console.enabled=${H2_CONSOLE_ENABLED:true}
spring.h2.console.path=${H2_CONSOLE_PATH:/h2-console}

# JPA / Hibernate
spring.jpa.database-platform=${DB_DIALECT:org.hibernate.dialect.H2Dialect}
spring.jpa.hibernate.ddl-auto=${DB_DDL_AUTO:update}
spring.jpa.show-sql=${DB_SHOW_SQL:true}

# Credenciales del admin inicial (bootstrap al primer arranque)
app.admin.username=${ADMIN_USERNAME:}
app.admin.password=${ADMIN_PASSWORD:}

# JWT
app.jwt.secret=${JWT_SECRET:}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# Ruta del fichero YAML de configuración runtime del servidor
app.config.path=${APP_CONFIG_PATH:#{systemProperties['user.home'] + '/.selfpotify/config.yml'}}

# Origen del frontend (CORS)
app.web.origin=${WEB_ORIGIN:http://localhost:3000}

# Last.fm API (clasificación de género automática)
app.lastfm.api-key=${LASTFM_API_KEY:}
app.lastfm.base-url=${LASTFM_BASE_URL:https://ws.audioscrobbler.com/2.0/}

# Carátulas/fotos automáticas (sin API key). COVER_ART_ENABLED=false las desactiva.
# COVER_ART_USER_AGENT identifica la app ante MusicBrainz (su política lo exige).
app.cover-art.enabled=${COVER_ART_ENABLED:true}
app.cover-art.user-agent=${COVER_ART_USER_AGENT:selfpotify/1.0 ( https://github.com/selfpotify )}
app.cover-art.connect-timeout-ms=${COVER_ART_CONNECT_TIMEOUT_MS:4000}
app.cover-art.read-timeout-ms=${COVER_ART_READ_TIMEOUT_MS:6000}

# Librería musical: ruta del host (fuera de Docker) y punto de montaje (en Docker).
# SELFPOTIFY_DOCKER lo fija el Dockerfile a true; fuera de Docker queda en false.
app.library.path=${MUSIC_LIBRARY_PATH:}
app.library.docker-path=${MUSIC_LIBRARY_MOUNT:/music}
app.library.docker=${SELFPOTIFY_DOCKER:false}

# Jackson: no fallar si un campo primitivo llega como null en el body
spring.jackson.deserialization.fail-on-null-for-primitives=false

# Límites de upload de multipart. El límite global de Spring debe cubrir el mayor
# de los dos usos (logo y audios), por eso se fija al tamaño de subida de canciones
# (mayor); el logo conserva su propio límite a nivel de aplicación (app.logo.*).
# max-request-size cubre la subida de varios audios en una sola petición.
spring.servlet.multipart.max-file-size=${SONG_UPLOAD_MAX_FILE_SIZE:50MB}
spring.servlet.multipart.max-request-size=${SONG_UPLOAD_MAX_REQUEST_SIZE:500MB}
app.logo.max-file-size=${LOGO_MAX_FILE_SIZE:2MB}
app.upload.max-file-size=${SONG_UPLOAD_MAX_FILE_SIZE:50MB}
